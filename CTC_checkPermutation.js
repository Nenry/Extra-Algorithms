//Given two strings, write a method that decides if one is a permutation of the other
//create two hash counters 
//iterate through each char seperately into its own hash counter
//iterate through one of the words again with each of its char, if char doesnt equal the same amount then return false